import "source-map-support/register";
declare class Artifactor {
    destination: string;
    constructor(destination: string);
    save(artifactObject: object): Promise<void>;
    saveAll(artifactObjects: any | Array<object>): Promise<void>;
}
export = Artifactor;
